{
  "name": "yesyes",
  "version": "0.0.1",
  "description": "Nonogram generator/solver",
  "main": "app.js",
  "scripts": {
    "minify:js": "babili src/js/app.js > dist/app.js",
    "minify:css": "minify src/css/app.css --output dist/app.css",
    "minify": "npm run minify:js & npm run minify:css",
    "vendor:dev": "cp src/js/vendor/vue.js dist/vendor/",
    "vendor:production": "cp src/js/vendor/vue.min.js dist/vendor/vue.js",
    "svg:generate": "svg-sprite-generate -d src/svg -o src/sprite/icons.svg",
    "copy:js": "cp src/js/app.js dist/",
    "copy:css": "cp src/css/app.css dist/",
    "copy:html": "cp src/index.html dist/",
    "copy:icons": "cp src/sprite/icons.svg dist/",
    "copy": "npm run copy:js && npm run copy:css && npm run copy:html && npm run copy:icons",
    "remove:dist": "rm -rf dist/*",
    "create:vendor": "mkdir dist/vendor",
    "clean": "npm run remove:dist && npm run create:vendor",
    "build:dev": "npm run clean && npm run copy && npm run vendor:dev",
    "build:production": "npm run clean && npm run copy:html && npm run copy:icons && npm run vendor:production && npm run minify",
    "watch": "watch 'npm run copy' src/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Musilitar/MusilitarAPI"
  },
  "keywords": [
    "nonogram",
    "picross",
    "solver",
    "generator"
  ],
  "author": "Lode Lesage",
  "license": "ISC",
  "devDependencies": {
    "babili": "0.1.4",
    "minifier": "0.8.1",
    "svg-sprite-generator": "0.0.7",
    "watch": "1.0.2"
  }
}
